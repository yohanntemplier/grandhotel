{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Chambres</h1>
    <ul>
        {% for room in rooms %}
            {{ room.name }} {{ room.description }} {{ room.price }} {{ room.area }}
        {% endfor %}
    </ul>

    {{ dump(rooms) }}
    <a href="../Item/add.html.twig">Ajouter</a>
{% endblock %}